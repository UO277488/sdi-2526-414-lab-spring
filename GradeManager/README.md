# GradeManager

## Ejecucion

La aplicacion se ejecuta en el puerto **8080** (Spring Boot por defecto).

URL de acceso local:

`http://localhost:8080`

## Cambio realizado

Se ha anadido el endpoint `POST /mark/add` en el controlador de notas para procesar el formulario de alta de una nueva nota y evitar el error `405 Method Not Allowed`.
